# SizeLook
It is used to test sizes of all directories
```
Step 1: Input your directory like this: 'E:\MyDirectory'(Windows); '/home'(Linux)
Step 2: Show the sizes of each directory/file in the directory
```
